# ──────────────────────────────────────────────────────────────────────────────
# requirements.txt – RigBridge Python-Abhaengigkeiten
# Versionen immer mit == pinnen fuer reproduzierbare Builds.
# ──────────────────────────────────────────────────────────────────────────────

# Core Dependencies
fastapi==0.111.0
uvicorn[standard]==0.30.1           # ASGI server for FastAPI
pydantic==2.7.1                     # Data validation and serialization
pyyaml==6.0.1                       # YAML parsing for protocol definitions
pyserial==3.5                       # USB/Serial communication
cryptography==42.0.5                # For encryption (secret storage)

# Utility
requests==2.32.2                    # HTTP client for Wavelog integration
typing-extensions>=4.12.0           # Type hints backport (required by pytest-asyncio 1.3.0)
websockets==12.0                    # WebSocket client for WaveLogGate integration

# Development Dependencies (Optional)
# Uncomment for development environments

pytest==8.2.2                       # Testing framework
pytest-asyncio==1.3.0               # Async test support (updated for TransportManager)
pytest-cov==5.0.0                   # Coverage reporting
httpx==0.27.0                       # HTTP client for FastAPI TestClient

# Code Quality
# ruff==0.4.4                       # Linter and formatter
# black==24.4.2                     # Code formatter
# mypy==1.10.0                      # Static type checker
